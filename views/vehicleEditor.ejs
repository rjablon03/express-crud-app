<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Vehicle Editor</title>
</head>
<body>
    <h1>Vehicle Editor</h1>

    <% if (vehicle.errors.length > 0) { %>
        <ul>
            <%- vehicle.errors.map((error) => `<li>${error}</li>`).join("\n"); %>
        </ul>
    <% } %>
    
    <%- include('vehicleForm') %>
</body>
</html>